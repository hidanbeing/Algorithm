n=int(input())
scores=list(map(int, input().split()))


candidates=[]
for i in range(n):
	candidates.append((scores[i], i+1))

candidates.sort(reverse=True)

print("%d %d %d"%(candidates[0][1],candidates[1][1],candidates[2][1]))
